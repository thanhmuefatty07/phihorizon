# =============================================================================
# Supreme System V5 - Environment Configuration Template
# =============================================================================
# SECURITY: Never commit .env with real credentials to version control.
# Usage: copy to .env and fill real values.
# =============================================================================

# === Exchange API Credentials ===
# Binance API (Required for live trading)
BINANCE_API_KEY=your_binance_api_key_here
BINANCE_API_SECRET=your_binance_api_secret_here
# Bybit API (Optional - multi-exchange support)
BYBIT_API_KEY=your_bybit_api_key_here
BYBIT_API_SECRET=your_bybit_api_secret_here
# MEXC API (Optional)
MEXC_API_KEY=
MEXC_API_SECRET=

# === Zero Trust Security ===
ZERO_TRUST_ENABLED=true
# Webhook URL for security event forwarding (e.g., Splunk, ELK)
SIEM_WEBHOOK_URL=
# Failed login lockout settings
LOCKOUT_THRESHOLD=5
LOCKOUT_DURATION_MINUTES=15

# === Database Configuration ===
DB_RETRY_MAX_ATTEMPTS=3
DB_RETRY_INITIAL_DELAY=0.1
DB_RETRY_BACKOFF_FACTOR=2.0
DB_PATH=data/paper_trading_oms.db

# === Trading Configuration ===
TRADING_MODE=paper
MAX_POSITION_SIZE=10000
MAX_DAILY_LOSS=500

# === Logging Configuration ===
LOG_LEVEL=INFO
LOG_FILE=logs/trading.log
LOG_MAX_SIZE_MB=100
LOG_BACKUP_COUNT=5

# === Monitoring & Alerts ===
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
METRICS_PORT=8000

# === Development ===
DEBUG=false
TESTING_MODE=false
TEST_MAX_ITERATIONS=

# =============================================================================
# NOTES:
# 1. Generate API keys from exchange websites (appropriate permissions).
# 2. Store this file securely (use secrets manager in production).
# 3. Rotate credentials regularly (90 days recommended).
# 4. Use read-only API keys for monitoring; read-write for trading.
# 5. Never share or commit credentials to version control.
# =============================================================================
